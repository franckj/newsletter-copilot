---
import { Image } from 'astro:assets';
import { about, sections } from '@/content/copy';
import juliet from '@/assets/juliet.png';

const s = sections.about;
---

<section class="section section-light" id="about">
  <div class="section-inner">
    <h2>{s.h2Prefix} <span class="accent">{s.h2Accent}</span></h2>
    <p class="lead">{about.intro}</p>

    <div class="about-grid">
      <div class="about-photo-wrap">
        <Image src={juliet} alt={about.name} width={254} height={254} class="about-photo" />
        <div class="about-card">
          <div class="name">{about.name}</div>
          <div class="role">{about.photoRole}</div>
        </div>
      </div>

      <div class="about-text">
        {about.paragraphs.map((p) => <p>{p}</p>)}

        <p>
          <strong>{about.paymentHeading}</strong>
          {' '}{about.paymentIntro}
        </p>

        <div class="about-benefits">
          {
            about.benefits.map((b, i) => (
              <div class="about-benefit">
                <div class="about-benefit-num">{i + 1}</div>
                <div class="about-benefit-text">
                  <strong>{b.title}</strong>
                  <span>{b.desc}</span>
                </div>
              </div>
            ))
          }
        </div>

        <p class="about-outro">{about.outro}</p>
        <p class="about-cta">{about.cta}</p>
        <p class="about-sign">
          <a href={about.linkedIn}>— {about.name}</a>
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  /* Match the intro to the body copy — one size across the whole section. */
  .section-inner > .lead {
    font-size: 1.06rem;
  }
  .about-grid {
    display: grid;
    grid-template-columns: 240px 1fr;
    gap: 64px;
    align-items: start;
    margin-top: 50px;
  }
  .about-photo-wrap {
    position: sticky;
    top: 30px;
  }
  .about-photo {
    width: 100%;
    height: auto;
    border-radius: 14px 14px 0 0;
    aspect-ratio: 1;
    object-fit: cover;
    display: block;
    border: 3px solid var(--pale);
    border-bottom: none;
  }
  .about-card {
    background: var(--navy);
    border: 3px solid var(--pale);
    border-top: none;
    border-radius: 0 0 14px 14px;
    padding: 14px 18px 16px;
    text-align: center;
  }
  .about-card .name {
    font-weight: 800;
    font-size: 0.95rem;
    color: #fff;
  }
  .about-card .role {
    color: var(--pale);
    font-size: 0.78rem;
    margin-top: 3px;
  }

  .about-text p {
    font-size: 1.06rem;
    color: var(--text-dark);
    line-height: 1.85;
    margin-bottom: 18px;
  }
  .about-text p strong {
    color: var(--navy);
    font-weight: 600;
  }

  .about-benefits {
    margin-top: 8px;
    display: grid;
    gap: 14px;
  }
  .about-benefit {
    display: grid;
    grid-template-columns: 32px 1fr;
    gap: 14px;
    align-items: start;
  }
  .about-benefit-num {
    width: 28px;
    height: 28px;
    border: 1px solid var(--red);
    color: var(--red);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
    font-size: 0.78rem;
    margin-top: 3px;
  }
  .about-benefit-text strong {
    display: block;
    color: var(--navy);
    font-weight: 700;
    font-size: 1rem;
    margin-bottom: 3px;
  }
  .about-benefit-text span {
    color: var(--muted-on-light);
    font-size: 1rem;
    line-height: 1.65;
  }

  .about-outro {
    margin-top: 24px;
  }
  .about-cta {
    color: var(--red);
    font-weight: 700;
    font-size: 1rem;
    margin-top: -4px;
  }
  .about-sign {
    font-weight: 700;
    font-size: 1rem;
    margin-top: 20px;
  }
  .about-sign a {
    color: var(--red);
    text-decoration: underline;
  }

  @media (max-width: 880px) {
    .about-grid {
      grid-template-columns: 1fr;
    }
    .about-photo-wrap {
      max-width: 240px;
      position: static;
    }
  }
</style>
